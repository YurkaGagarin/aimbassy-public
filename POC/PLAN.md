# PLAN — чек-лист разработки (POC)

«Чек-лист ремонта»: задачи со статусами. Легенда: `[ ]` не начато · `[~]` в работе · `[x]` готово.
Полный недельный план с обоснованием — в `../04_План_разработки_на_неделю.md`. Здесь — рабочий трекер. Подробности выполнения каждого пункта пишем в MEMORY.md.

## Фаза 0 — Каркас проекта
- [x] Создать `Initial/POC/` + служебные файлы (CONTEXT, PLAN, MEMORY)
- [x] CLAUDE.md — собран, прогнан через `claude-md-improver` (B/77, улучшен), одобрен пользователем
- [x] git init (ветка `main`, корень `Initial/`, автор abirisr) + .gitignore + первый коммит `36c7e97`

## День 1 — Фундамент данных + GCP — ВЫПОЛНЕНО (2026-06-09)
Цель к вечеру: чанки с метаданными в Chroma, поиск что-то возвращает. Достигнуто: 163 куска в Chroma, retrieval релевантен на 5 RU-вопросах.
- [x] 1. GCP: Vertex AI включён; доступ подтверждён вживую — `gemini-2.5-pro/flash/flash-lite` + `gemini-embedding-001` (200). Изолированный конфиг `ambassy`. Готча: user-креды требуют `X-Goog-User-Project`
- [x] 2. Корпус → Markdown в `POC/data/`: NAG/StbG (`pandoc rtf→markdown`, тело чистое), FPG_88 (из PDF, есть мусор сайта), 2 кейса (docx→md). Чистка шапки/оглавления — на шаге 3-4
- [x] 3. Чистка корпуса + схема метаданных. Шапки срезаны (NAG/StbG, тело `diff` IDENTICAL); FPG почищен (letters-only IDENTICAL 2040). Карточка: `country/doc_type/law_code/paragraph/para_sort/section/title/lang/source_url`; URL из Sources.xlsx (NAG/StbG → RIS, FPG → JUSLINE deep)
- [x] 4. Нарезка (`chunk_corpus.py`, NBSP-aware, без overlap) → `data/chunks.jsonl`: 163 куска (NAG 86, StbG 74, FPG 1, 2 обезличенных кейса), проверено глазами. Кейсы анонимизированы (оригиналы целы), утечка PII = 0. Длины: median 1292 / max 19955 (28 >3000 → до-резка как рычаг Дня 4)
- [x] 5. Эмбеддинг `embed_index.py` (gemini-embedding-001, dim 768, RETRIEVAL_DOCUMENT, autoTruncate; gcloud-токен + quota-header) → ChromaDB `data/chroma` (cosine), 163 куска. API сверены context7 + живой тест
- [x] 6. Smoke `smoke_test.py`: 5 RU-вопросов, кросс-язык работает (гражданство→StbG-10/11a, Daueraufenthalt→NAG-45, RWR→NAG-41, без паспорта→CASE-2/NAG-24, Fremdenpass→FPG-88). Релевантность отличная

## День 2 — Первый сквозной ответ + демо — ВЫПОЛНЕНО (2026-06-09)
Цель достигнута: «вопрос RU → ответ с § и дисклеймером», видно в Streamlit. Модель baseline — gemini-2.5-pro. Коммит `6be0f7b`.
- [x] 1. Окружение: доустановлены `langgraph 1.2.4` + `streamlit 1.58.0` в venv; импорт ок, Chroma-конвейер не сломан (pandas 3.0.3 ↔ chromadb 1.5.9 уживаются)
- [x] 2. Системный промпт + few-shots (`prompts.py`, утверждён): мультистрановость заложена (не хардкодим немецкий/Австрию), few-shots динамические по коду страны (`select_fewshots`), дисклеймер детерминированно из кода, анти-leakage от 13 тест-вопросов
- [x] 3. Ядро `rag_core.py`: `retrieve` (top-k из Chroma, фильтр по country) + `generate` (gemini-2.5-pro, format сверён context7) + `answer`. Грабля: 2.5-pro «думает» → подняли maxOutputTokens 2048→8192 (иначе обрыв по MAX_TOKENS). Живой ответ с точными § — ок
- [x] 4. LangGraph-граф `graph.py`: `retrieve → generate`, типизированный `RagState`, `ask()`/`graph.invoke`. API сверён context7. Без checkpointer (Simplicity First, добавим под диалог). Токен берём раз и прокидываем через стейт. Ответ разделяет закон/практику — ок
- [x] 5. Streamlit «рентген-панель» `app.py`: форма вопроса, ответ слева, найденные куски справа (expander: §/закон/дистанция/ссылка/текст), выбор страны + top-k. Идиомы сверены context7; headless-старт → HTTP 200. Запуск: `streamlit run app.py`
- [x] 6. Прогон на 3 вопросах (гражданство / Daueraufenthalt / провокационный «пошлина — нет в корпусе»): §-ссылки + дисклеймер на месте, провокационный честно «нет суммы, не выдумал». Коммит `6be0f7b`. Privacy-чистка: убраны упоминания городов кейсов из журнала

## День 3 — Evaluation harness — ВЫПОЛНЕНО (2026-06-10)
СТАТУС: baseline зафиксирован. Главный результат — Hit@5=0.14/MRR=0.21 (retrieval — узкое место) + answer-панель (grounding 4.46 высоко, correctness 3.05 упирается в retrieval, отказы 2.67 слабо, self-preference +0.93).
- [x] Расширить корпус новыми законами волонтёров: AsylG 2005 + полный FPG + AuslBG + NAG-DV → **442 куска** (6 источников), переэмбеддены, smoke ок. NAG-DV закрыл q13 (но `NAG-DV-7` на ранге 8 — top-5 промах). Коммиты `68a84c6`, +AuslBG, +NAG-DV
- [x] Собрать `eval/testset.jsonl` (13 вопросов: 7 в корпусе, 6 вне = честный отказ). `build_testset.py`, маппинг утверждён
- [x] Прогон 13 вопросов через пайплайн (`run_answers.py`, все 13 OK)
- [x] Метрики: retrieval (`run_retrieval.py` → Hit@5 0.14, MRR 0.21, SICRT) + answer-панель 3 судей (Gemini+GPT-5.5+Claude, `judges.py`/`score.py`), self-preference дельта +0.93 замерена
- [ ] Расширить тест-сет синтетикой до ~30-50 (отложено — реальных 13 хватило для baseline; синтетику добавим при росте корпуса)
- [x] Зафиксировать baseline-числа (`baseline_retrieval.md`, `baseline_answers.md`)

## День 4 — Анализ + одно улучшение — ВЫПОЛНЕНО (2026-06-10)
Рычаг: кросс-язычный query rewrite (RU→formal DE, gemini-2.5-flash). Решение по порядку узлов: rewrite ПОСЛЕ guardrail.
- [x] Диагноз: поиск (retrieval), не генерация — подтверждён числом Дня 3 (grounding высокий, correctness упирается в retrieval)
- [x] Рычаг 1 = query rewrite. Retrieval: Hit@5 **0.14→0.57 (×4)**, MRR 0.21→0.56; замер по 5 прогонам (перевод недетерминирован даже при temp=0). RRF(RU+DE) отброшен (хуже чистого DE). `exp_query_rewrite.md`
- [x] Таблица до/после answer-quality (`baseline_vs_rewrite.md`): in-corpus correctness 2.90→3.38 (+0.48), behavior 4.43→4.81; но out-of-corpus отказы регрессировали 2.67→2.06 (-0.61) → мотивация Дня 5. Переводы заморожены для чистого A/B
- [x] Рычаг 2 = обогащение корпуса (идея учителя, 2026-06-13): теги + RU-резюме + 3 синтетических RU-вопроса на чанк (эмбедим обогащённое, генерируем из оригинала). До-резка 27 толстых §§ (`rechunk.py`, потерь нет), генерация (`enrich.py`), анти-leakage (`enrich_check.py`, 1 дубль убран), отдельная коллекция (`embed_enriched.py`). **A/B Hit@5: cur+de 0.57 → enr+de 0.71; enr+ru (без перевода) 0.57 = весь рычаг Дня 4.** Победы q04 26→6, q12 19→3. Издержки: Hit@1 0.43→0.29, q03 нужен перевод. **Answer-панель (gate, `compare_enriched_answers.py`→`enriched_answers.md`, панель {gemini, claude} — GPT без ключа): overall ALL 3.58→4.08, in-corpus 4.14→4.64, refuse 2.92→3.42, grounding плоский; регрессия только q06 (паспорт) 3.50→2.00.** Гейт пройден → **живой индекс переключён на обогащённый** (`rag_core.SERVING_COLLECTION`, env `AIMBASSY_COLLECTION`, откат одной переменной). Проверено вживую (554, StbG §10 ранг 1). Follow-up: q06, вернуть GPT-судью
- [x] (data, 2026-06-13) Новые кейсы Насти 3-5 в корпус: docx→pandoc→обезличены (`case3/4/5_anon.md`, gitignored)→CASE-3/4/5 в `chunk_corpus.py`→конвейер (445 чанков, коллекция 557). Анти-leakage 0 флагов; retrieval enr+de Hit@5 держится 0.71 (без регрессии). Замер: кейсы всплывают для q05/q02/q11/q10 ТОЛЬКО при RU-запросе, DE-rewrite их не видит → мотивирует рычаг RRF(RU+DE), который и закроет q11/q02
- [x] (tooling, 2026-06-13) Фикс .env-судьи: `eval/judges.py` грузит `POC/.env` явно (был `find_dotenv` → брал ближайший без `OPENAI_API_KEY`). `GEMINI_API_KEY` не нужен (Vertex по gcloud-токену)
- [x] Рычаг 1 (порядок пользователя RRF→reranker→Flash): RRF(RU+DE) двойной поиск (`rag_core.retrieve_rrf`, c=60; `graph.use_rrf`; `eval/eval_rrf.py`). Переоткрыт после смены условий (Дня 4 отказ был при RU 0.14, теперь enr+ru 0.57). **A/B (frozen DE одинаков): Hit@1 0.29→0.43, MRR 0.46→0.51, кейсы в top-5 2→3 (q11←CASE-2); цена — q03/AuslBG-4 размыт 3→6 (Hit@3/5 0.71→0.57).** Маржинальный нетто-плюс; c не подгонял под n=7 (q03@c=5 = лезвие ножа). Кейс-бонус фрагилен (живой rewrite недетерминирован). Net на ответы — answer-панелью после reranker
- [x] Рычаг 2 = Flash-реранкер (`rerank.py`, узел в графе, `graph.use_rerank`, `eval/eval_rerank.py`). Движок: Flash-судья (кросс-язык нативно, 0 новых зависимостей, прод-переносим) вместо torch-cross-encoder/Vertex Ranking API. **A/B (frozen DE, n=7): enr+rrf→enr+rrf+rerank Hit@3/5 0.57→0.71 (починил регрессию RRF), Hit@10 0.71→0.86, MRR 0.51→0.60.** Звезда: q04 ранг 32→17→1. Кейсы в top-5: 3→4 (добавил q05/refuse, кросс-язык). Грабля токенов (рецидив): думание 1967>cap 2048 → MAX_TOKENS → обрыв JSON → fail-safe тихо возвращал исходный порядок (поймано через «слишком чистый» результат); фикс 2048→4096. Остаток: q03 не спасён (6→6), латентность ~12с (thinkingBudget — follow-up). **Итог стека RRF+rerank vs enr+de: Hit@1 +0.14, Hit@5 без регрессии, Hit@10 +0.14, MRR +0.14, кейсы 2→4**
- [x] Answer-панель на стек RRF+rerank vs enr+de (запрос пользователя, до рычага 3; 3 судьи gemini+gpt+claude, GPT добран на enr+de; `exp_answer_stack.py`/`compare_stack_answers.py`→`stack_answers.md`). **РАЗВОРОТ: ответы просели — overall ALL 3.74→3.44, out/refuse 3.22→2.67 (behavior -0.72); in-corpus почти ровно 4.19→4.10.** Рецидив связки Дня 4 (лучший поиск → уверенный ответ вместо отказа). q03 — РЕАЛЬНЫЙ промах (3.67→3.00), не артефакт. Оговорка: судился СЫРОЙ генератор без роутера (роутер сделан гасить это). Дефолты не трогал
- [x] Переразметка эксперта (Настя, 2026-06-13): мой бриф с §/Abs из корпуса → Настя приняла 4 из 6 в `Test Qs.xlsx` (q02/q05/q06/q11 refuse→answer), оставила q01/q08. `build_testset.py MAPPING` обновлён, testset 7/6 → **11 answer / 2 refuse**. Пере-судил (`rejudge_relabel.py`). **РАЗВОРОТ Day-4/5: «-0.56 на refuse» был артефактом метки.** PRO panel in-corpus 4.00→4.00 (behavior +0.24); routed in-corpus 3.58→4.00 (+0.42), ALL +0.26. Прошлый тезис «роутер обманут на q05» снят (q05 отвечаем). Остаток refuse = q01/q08; q08 — роутер всё ещё отвечает (реальный промах)
- [x] РЕШЕНИЕ по RRF+rerank: роутнутый вывод дозамерен (last session + переснят на relabeled) → **keep on** (net-плюс для in-corpus, ядро). q03 (top-k / заметка НП) и q06 (gold вытеснен) — открытые follow-up поиска
- [x] Рычаг 3 = Flash на генерацию (`rag_core.generate(model=)`, дефолт Pro не тронут; `exp_answer_flash.py` Pro vs Flash на одном ретривале; `compare_flash_answers.py`→`flash_answers.md`). **Латентность 23.8с→12.5с (≈2x, `flash_latency.md`); качество in-corpus overall 4.00→3.97 (−0.03, вровень), grounding +0.21.** ВЕРДИКТ: брать Flash на генерации. **Дефолт переключён на Flash (commit d8e27b9, env `AIMBASSY_GEN_MODEL` для отката на Pro).** Follow-up: thinkingBudget (Flash gen разброс 4-20с)
- [x] Дозамер routed (user-facing) на Flash vs Pro (`eval/route_flash.py`→`routed_flash.md`, тот же RRF+rerank). **Сырая панель вровень (in-corpus 4.00→3.97), а routed просел: in-corpus 4.00→3.64 — вся просадка в q09/q11 (роутер на Flash → handoff).** НАЙДЕН+ЗАКРЫТ баг: судья-роутер `maxOutputTokens=2048` падал на длинном ответе Flash q11 (`JSONDecodeError`, детерминированно 5/5), `route()` fail-safe ловил → handoff/conf0 = краш маскировался под честный отказ. Фикс **2048→8192** (`router.py`). Перемер: цифры те же (q11 теперь настоящий answerable=False conf4). КОРЕНЬ q09/q11 = роутер судит по ОБРАМЛЕНИЮ, не контенту (оба ответа дают тот же путь, Flash точнее цитирует §19/8; панель q11 = 4.33). Вердикт: дефолт Flash ОСТАЁТСЯ; routed-гэп — отдельный рычаг (ниже)
- [x] Рычаг роутера (A) + лог ошибок (C): рубрика роутера v3 — добавлено scoped-правило HONEST-REDIRECT (ложная посылка + реальный путь = answerable). Итер.1 (широкая, с переформулировкой «underlying need») чинила цель, но давала коллатераль (q01-Pro/q02-Flash, стабильно) → откатил глобальную часть, оставил scoped-исключение. **Итог (стабильно 2 прогона): q09+q11 на Flash починены без коллатерали; q01 осциллирует answer/handoff при temp=0 (граница, шум). Flash routed in-corpus 3.64→3.97 (вровень Pro 4.00), ALL 3.54→3.82 (Pro 3.87) — гэп закрыт.** C: `route()` возвращает `error` (краш судьи vs реальный вердикт), `graph._safe_detail` пишет в `flow_events`. Метод-вывод: LLM-судья не тюнится хирургически (глобальный дрейф), фикс найден замером; n=13 имеет шумовой пол (q01 флипает) → нужен held-out eval + A/B. B (обрамление) НЕ брали (порочный круг). Файлы: `router.py`, `graph.py`, `eval/routed_flash.md`
- [ ] ТОЧКА РЕШЕНИЯ Flash-vs-Pro (теперь СИЛЬНО в пользу Flash): после рычага роутера Flash routed ≈ Pro (ALL 3.82 vs 3.87, in-corpus 3.97 vs 4.00) при 2x скорости. Pro остаётся фолбэком одной env. Финальное подтверждение — A/B на реальных юзерах (панель/роутер — прокси). Открытый остаток refuse: q08 роутер всё ещё отвечает; q01 на границе

## День 5 — Guardrail + Router — ВЫПОЛНЕНО (2026-06-12; обоснован -0.61 на отказах)
- [x] 5.1 Guardrail: DLP (PII, `deidentify`) + Flash-Lite (тема/инъекция) → структурный вердикт. `guardrail.py`, коммит `222de06`. DLP включён+проверен вживую на RU/BY. Контакт координатора = заглушка
- [x] 5.2 Мини-eval guardrail на 40 синт. примерах → 3 confusion matrix: тема 1.00, инъекция 1.00, PII Recall 1.00/Prec 0.83 (`eval/guardrail_eval.md`)
- [x] 5.3 Router: `router.py` — Flash-судья (grounded+actionable) после генерации → ниже порога → human-handoff (контакт = заглушка, fail-safe на ошибке). Грабля: maxOutputTokens 512→2048 («думающий» Flash резал JSON → conf=0-артефакт, пойман)
- [x] 5.4 Петля `eval/run_router.py` на 13: in-corpus **7/7** отвечены (без регрессии), out-of-corpus поймано **2/6**. Рубрика v2>v1 (1/6→2/6, +q05), устойчиво в 3 прогонах. ВЫВОД: q02/q11 (вредные) роутер не ловит структурно — чистое обоснованное «нет» = валидный ответ для судьи; это пробел ПОЛНОТЫ КОРПУСА, лечится заметками НП (бэклог), не судьёй. v1/v2 отчёты сохранены для A/B
- [x] 5.5 Полный граф `guardrail → rewrite → retrieve → generate → router → END` в `graph.py`. **5.5a** (`ab2bdfc`): guardrail (refuse → короткое замыкание, Pro не тратится) + router (final = ответ|handoff); вердикты в Streamlit. **5.5b**: rewrite вынесен в канонический `rewrite.py` (eval не тронут), тумблер rewrite в Streamlit (живое Day-4 до/после) + DE-запрос; graceful-fallback на сбое. Инвариант: scrubbed течёт вниз (PII не в поиск/модель), generate/router на RU, retrieve на DE. Проверено вживую (allow/refuse/PII/toggle-ветки, headless 200)

## День 6 — Telegram + GCP-прод — ЯДРО ВЫПОЛНЕНО (2026-06-12; 6.4 отложен осознанно)  ← ТЕКУЩИЙ
- [x] 6.1 `telegram_bot.py` (polling, PTB 22.7, API сверён context7): `/start` + текст → `g.ask()` в worker-треде (`asyncio.to_thread`), ответ `final`, typing-индикатор, разбивка >4096, отказ/handoff из графа. Бот `@AImbassy_bot` стартует и поллит чисто (getMe 200)
- [x] 6.2 `bq_log.py` → BigQuery `ambassy.interactions` + `guardrail_events` (REST insertAll, gcloud-токен + quota-header): только scrubbed-текст + метрики, `user_id` хешируется (sha256), оригинал не хранится. Self-disabling (логи не валят бота). Проверено: строки легли (answer/refuse + allow/refuse)
- [x] 6.3 Rate-limit: in-memory счётчик 8 сообщений/60с на пользователя (прод → Firestore)
- [ ] 6.4 Cloud Run (webhook) — ОТЛОЖЕНО осознанно: прод-деплой требует миграции Chroma→managed vector store (ещё не сделана), а polling достаточно для рабочего демо/видео. Документировано как стретч
- [x] 6.5 Пер-узловой трейс (наблюдаемость): канал `trace` (`operator.add`) + `request_id` в `graph.py`, обёртка `traced()` (latency+status+приватный detail на узел) → BigQuery `flow_events` (1 строка/узел, в `bq_log`), панель трейса в Streamlit x-ray. Выбор flow_events vs OTel/LangSmith (LangSmith отвергнут — внешний SaaS, приватность). Проверено вживую: бутылочное горлышко generate ~22с + router ~7с; строки в `flow_events` подтверждены
- Грабля (поймана): httpx логировал URL Telegram с ТОКЕНОМ на INFO → заглушили httpx-логгер до WARNING, лог удалён. Осталось: живой диалог в Telegram (шаг пользователя)

## День 7 — Отчёт, питч, подстраховка
- [ ] Отчёт по разделам задания
- [ ] Слайды (8 мин)
- [ ] Видео-демо (обязательно)
- [ ] Чистовая диаграмма архитектуры

## Кольцо C — только описать в архитектуре (не кодить)
Эскалирующие баны · интерактивные уточнения · ReAct + веб-поиск · Литва (вторая страна) · court decisions

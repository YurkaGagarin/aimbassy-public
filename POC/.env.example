# Copy this file to `.env` (which is gitignored) and fill in the values.
# NEVER commit real secrets — only this .example template is tracked.

# Telegram bot token from @BotFather (https://t.me/BotFather). SECRET.
TELEGRAM_BOT_TOKEN=

# Human coordinator contact shown to the user on a router hand-off.
# Falls back to "@coordinator_placeholder" if unset.
AMBASSY_COORDINATOR=
